requests==2.31.0
tenacity==8.2.3

# étape 2 (analyse des documents d'urbanisme) — non versionnés précisément :
# à figer avec `pip freeze` après une première installation réussie.
anthropic
python-dotenv
pdfplumber
pytesseract
pdf2image

# étape 4 (construction des géométries) — non versionnés précisément, même
# raison qu'étape 2 : à figer avec `pip freeze` après une première
# installation réussie.
geopandas
pyogrio
shapely

# étape 5 (rédaction des messages) — vérification orthographique/grammaticale
# des messages corrigés manuellement (verifier_orthographe.py). Installe à la
# demande, au premier import, sa propre dépendance Grammalecte-fr (pas de
# JVM, entièrement hors ligne une fois installée).
pygrammalecte
